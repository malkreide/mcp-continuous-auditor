# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Node / promptfoo
node_modules/
.promptfoo/
promptfoo-output.*

# Audit work dir — cloned target source + run logs (scripts/audit-target.sh)
.audit/

# Phase-5 rollout artifacts — base images, per-run overlays, seeds, rendered
# cloud-init. (The per-connection handler _receive-one.sh is now a committed
# source file, exercised by tests/test_broker_pipeline.py — no longer generated.)
deploy/.images/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
